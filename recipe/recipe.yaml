context:
  version: "0.22.0"

package:
  name: ndonnx
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/n/ndonnx/ndonnx-${{ version }}.tar.gz
    sha256: d311283193e0f2e092a9d406e426de127c161b1b43a6b4df91683cdc4005bdca

build:
  script: ${{ PYTHON }} -m pip install .
  noarch: python
  number: 0

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools
    - setuptools-scm
    - pip
  run:
    - python >=${{ python_min }}
    - numpy
    - onnx
    - spox >=0.10
    - typing-extensions

tests:
  - python:
      imports:
        - ndonnx
      python_version:
        - ${{ python_min }}.*
        - "*"
      pip_check: true

about:
  homepage: https://github.com/quantco/ndonnx
  summary: ONNX backed array library compliant with Array API standard.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - cbourjau
    - sgilm
